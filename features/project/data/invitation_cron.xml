<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!--
        ═══════════════════════════════════════════════════════════════
        ═         🔄 PROJECT INVITATION CRON JOBS                     ═
        ═══════════════════════════════════════════════════════════════
        -->

        <!-- Cron job để tự động expire các invitation hết hạn -->
        <record id="cron_expire_project_invitations" model="ir.cron">
            <field name="name">Expire Project Invitations</field>
            <field name="model_id" ref="model_vnfield_project_invitation" />
            <field name="state">code</field>
            <field name="code">model._cron_expire_invitations()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True" />
            <field name="doall" eval="False" />
            <field name="user_id" ref="base.user_root" />
        </record>

    </data>
</odoo>