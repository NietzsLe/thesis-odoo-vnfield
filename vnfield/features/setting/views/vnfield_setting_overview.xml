<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
    ═══════════════════════════════════════════════════════════
    ═         🏠 VNFIELD SETTING - OVERVIEW SCREEN           ═
    ═══════════════════════════════════════════════════════════
    Giao diện tổng quan chỉ xem, cho phép điều hướng đến các menu con
    -->
    <record id="view_vnfield_setting_overview" model="ir.ui.view">
        <field name="name">vnfield.setting.overview</field>
        <field name="model">ir.ui.menu</field>
        <field name="arch" type="xml">
            <form string="VN Field Settings Overview" create="0" edit="0" delete="0">
                <sheet>
                    <field name="name" invisible="1" />
                    <h2>VN Field Settings - Tổng quan</h2>
                    <div class="alert alert-info" role="alert">
                        Đây là giao diện tổng quan. Chọn chức năng bên dưới để điều hướng nhanh đến
                        các cấu hình quan trọng.
                    </div>
                    <!-- Không dùng field dashboard_info vì model ir.ui.menu không có field này -->

                    <div class="d-flex flex-row gap-3 mt-2">
                        <button type="action" name="vnfield.action_kafka_cron_manager"
                            string="Kafka Consumer Management" class="btn btn-primary" />
                        <button type="action" name="vnfield.action_kafka_config_wizard"
                            string="Kafka Configuration" class="btn btn-secondary" />
                        <button type="action"
                            name="vnfield.action_system_type_config_wizard"
                            string="System Type Configuration" class="btn btn-info" />
                        <button type="action"
                            name="vnfield.action_contractor_representative_wizard"
                            string="Contractor Representative" class="btn btn-success" />
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_vnfield_setting_overview" model="ir.actions.act_window">
        <field name="name">VN Field Settings Overview</field>
        <field name="res_model">ir.ui.menu</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_vnfield_setting_overview" />
        <field name="target">current</field>
        <field name="res_id" ref="vnfield_setting_main_menu" />
        <field name="context">{}</field>
    </record>

    <record id="vnfield_setting_main_menu" model="ir.ui.menu">
        <field name="name">VN Field Settings</field>
        <field name="sequence">12</field>
        <field name="web_icon">vnfield,static/description/icon.png</field>
        <field name="action" ref="action_vnfield_setting_overview" />
    </record>
</odoo>